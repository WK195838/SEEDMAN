             PGM
             RSTLIB     SAVLIB(PTCFLIB) DEV(TAP01) ENDOPT(*LEAVE) +
                          MBROPT(*ALL) ALWOBJDIF(*ALL)
             RSTLIB     SAVLIB(PTCPLIB) DEV(TAP01) ENDOPT(*LEAVE) +
                          MBROPT(*ALL) ALWOBJDIF(*ALL)
             RSTLIB     SAVLIB(CKFLIBAU) DEV(TAP01) ENDOPT(*LEAVE) +
                          MBROPT(*ALL) ALWOBJDIF(*ALL)
             RSTLIB     SAVLIB(CKPLIBAU) DEV(TAP01) ENDOPT(*LEAVE) +
                          MBROPT(*ALL) ALWOBJDIF(*ALL)
             RSTLIB     SAVLIB(GLFLIBAU) DEV(TAP01) ENDOPT(*LEAVE) +
                          MBROPT(*ALL) ALWOBJDIF(*ALL)
             RSTLIB     SAVLIB(GLPLIBAU) DEV(TAP01) ENDOPT(*LEAVE) +
                          MBROPT(*ALL) ALWOBJDIF(*ALL)
             RSTLIB     SAVLIB(GLCKPLIBAU) DEV(TAP01) ENDOPT(*LEAVE) +
                          MBROPT(*ALL) ALWOBJDIF(*ALL)
             RSTLIB     SAVLIB(CKSRCSB) DEV(TAP01) ENDOPT(*LEAVE) +
                          MBROPT(*ALL) ALWOBJDIF(*ALL)
             RSTLIB     SAVLIB(GLSRCSN) DEV(TAP01) ENDOPT(*LEAVE) +
                          MBROPT(*ALL) ALWOBJDIF(*ALL)
             RSTLIB     SAVLIB(GLCKFLIBAU) DEV(TAP01) ENDOPT(*LEAVE) +
                          MBROPT(*ALL) ALWOBJDIF(*ALL)

             ENDPGM
